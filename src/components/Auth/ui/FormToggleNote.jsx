import React from 'react';
import '../../../styles/Auth/ui/FormToggleNote.css';

const FormToggleNote = ({ children }) => {
  return (
    <div className="hok-auth-form-toggle-note">
      {children}
    </div>
  );
};

export default FormToggleNote;