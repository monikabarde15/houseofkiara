# Dispatch — spec_miner_frontend_1

## Task: Frontend Codebase & Mock Data Specification Mining
1. Read `d:/HOKAdmin/hok_admin/.agents/ORIGINAL_REQUEST.md`.
2. Inspect frontend structure:
   - Check `package.json`, `vite.config.*`, `src/App.tsx` or `src/App.jsx`, `src/main.tsx`.
   - Inspect views in `src/pages/` and `src/components/` for Operations (Orders, Offers, Enquiries, Rental Calendar, Dispatch Schedule, Returns & Deposits, Payouts, Customers) and Catalogue (Products, Designers, Listers, LYP) and Auth/Login.
   - Inspect `src/data/`, `src/mock/`, `src/services/`, `src/api/` for mock datasets and API callers.
3. List:
   - All mock files to be removed/replaced.
   - All API client endpoints expected by frontend views.
   - Any state management or auth context details (e.g., token in localStorage, headers).
4. Write detailed findings to `d:/HOKAdmin/hok_admin/.agents/spec_miner_frontend_1/analysis.md` and handoff report to `d:/HOKAdmin/hok_admin/.agents/spec_miner_frontend_1/handoff.md`.
5. Send message to parent upon completion.
