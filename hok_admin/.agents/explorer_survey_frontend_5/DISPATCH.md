# Dispatch — explorer_survey_frontend_5

## Mission
Survey frontend code in `d:/HOKAdmin/hok_admin/src` to catalog:
1. All Operations views (Orders, Offers, Enquiries, Calendar, Dispatch, Returns, Payouts, Customers) & Catalogue views (Products, Designers, Listers, LYP) & Admin Auth.
2. All mock data files (e.g. `mockOrders.ts`, `mockProducts.ts`, etc.) and mock arrays that must be removed.
3. The API client layer (`api/`, `services/`, etc.) and exact endpoints needed to connect to the backend without altering any UI styling or layout.

## Output
Write `d:/HOKAdmin/hok_admin/.agents/explorer_survey_frontend_5/analysis.md` and `handoff.md`.
Send message to parent when done.
